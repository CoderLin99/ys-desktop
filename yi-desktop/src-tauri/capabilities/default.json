{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "桌面与安卓共用：事件与窗口默认权限（HTTP 走 Rust 命令，不开放任意 URL）。",
  "windows": ["main"],
  "platforms": ["linux", "macOS", "windows", "android", "iOS"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default"
  ]
}
